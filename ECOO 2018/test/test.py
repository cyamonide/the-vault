'''
ECOO Python 2 Template
By: Andrew Wang

Intended usage is to use the comments as guidelines 
rather than working lines of code in the program. 

You should probably delete all of the comments you don't
need so as to not cluster fuck your program.
'''
import math, itertools, time

with open("DATA00.in") as input:
# SPECIFICATION INT'S IN-LINE
    # a, b, c = [int(x) for x in next(input).split(' ')]

# DATA LIST IN-LINE 
    # arr = list(map(int, next(input).split(' ')))

# CONTINUOUS INPUT UNTIL SENTINEL_VALUE
# (ENSURE THAT THE SENTINEL IS STRING TYPED)
    '''
    found = False
    while not found:
        s = next(input)
        if s == 
0
:
            found = True
        else:
            ##### ALGORITHM CODE HERE
    '''
    input.close()