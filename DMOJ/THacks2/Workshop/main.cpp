#include <iostream>
#include <math.h>
#include <vector>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int a;
    cin >> a;

    int a = n/m;

    long price;
    long thisPrice;

    while(true) {
        thisPrice =
    }

    // a^b mod c = (a mod c)^b mod c

    return 0;
}