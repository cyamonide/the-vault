#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

    int N,M,P;

    cin >> N >> M >> P;

    if (M == N) {
        cout << "Switch" << endl;
    } else {
        cout << "Stay" << endl;
    }

	return 0;
}
