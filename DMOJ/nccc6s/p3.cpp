#include <bits/stdc++.h>

using namespace std;

int main() {
    int X, Y, N, xs, ys, xe, ye, xi, yi;
    cin >> X >> Y >> N >> xs >> ys >> xe >> ye >> xi >> yi;

    cout << -1 << endl;

    return 0;
}

// Closed bounds between (0, 0) and (X, Y)
// Change in velocity happens before the helicopter moves in that second
// Change in velocity can be only done by one unit in x or y direction
